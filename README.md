# HexTactics SRV

Complete ESX Legacy server bundle for HexTactics, including the CityCentral municipality configuration and txAdmin recipe.

## Installation

Use `recipe.yaml` in txAdmin or follow `INSTALLATIE.md` for a manual setup.
